---
interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---
<div class="bg-surface2 p-4 rounded-card">
  <p class="font-mono font-medium text-lg text-gold">{value}</p>
  <p class="text-sm text-ink-muted mt-0.5">{label}</p>
</div>
